// eslint-disable-next-line import/no-unresolved
import * as lifeFiles from './life-files';

const currentRules = lifeFiles.readWorldConfig('./src/world-rules.json');
const mainArray: number[][] = [];


switch (key) {
  case value:
    
    break;

  default:
    break;
}
